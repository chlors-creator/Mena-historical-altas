# 《沙海纪年》项目上下文

> 后续对话开始时优先阅读本文件。它记录当前项目状态、目录约定和操作规则，避免重复扫描整个项目。

## 1. 项目概览

- 项目名称：沙海纪年（MENA Historical Atlas）
- 内容范围：1797—2026 年中东与北非历史互动地图
- 主要语言：简体中文；少量英文与阿拉伯文用于界面辅助
- 技术形态：无构建步骤的静态网站，原生 HTML、CSS、JavaScript、SVG
- 发布方式：仅维护可手动上传到 GitHub Pages 的静态文件，不使用 Sites 发布。
- 视觉方向：历史档案与旧地图风格，沙色纸张、暗红强调色、衬线字体

## 2. 目录约定

### 正式工作目录与 GitHub 手动上传目录

`mena-historical-altas/`

后续直接在此目录修改。用户说“同步到 GitHub”时，只需确保该目录的根文件和 `dist/` 副本一致；保留 `.git`，不执行 `git push`，不代替用户上传。

### 不应同步的临时内容

- `.sites-package-stage*/`
- `*.tar.gz`
- 本地预览截图
- `natural-earth-50m-countries.geojson`（原始转换素材；成品已写入 `mena-2026.js`）

## 3. 核心文件

| 文件 | 作用 |
| --- | --- |
| `dist/index.html` | 页面结构、内嵌历史示意地图、时间轴、右侧信息面板、关于弹窗、favicon |
| `dist/styles.css` | 全部视觉样式、地图状态、响应式布局、时间轴定位 |
| `dist/app.js` | 年代数据、国家／地区资料、交互逻辑、地图着色、播放与跳转 |
| `dist/mena-2026.js` | 2026 年 Natural Earth 国界转换后的 SVG 路径数据 |
| `mena-historical-1886-1923.js` | CShapes 2.0 1886—1923 年度历史边界数据 |
| `mena-historical-1924-1999.js` | CShapes 2.0 1924—1999 有效期区间压缩后的年度历史边界数据 |
| `mena-historical-aden.js` | 1886—1937 亚丁与东亚丁保护地几何回补（1938 起沿用 CShapes 原生区间） |
| `mena-historical-yemen-extension.js` | 旧版北也门延伸数据（现版本不再加载，避免把统一也门轮廓倒填到北也门） |
| `formal-names.js` | 各国家／历史实体按年份映射的正式国名 |
| `PROJECT_CONTEXT.md` | 本文件；供后续对话快速恢复上下文 |

## 4. 当前功能

- 时间滑块覆盖 1797—2026，每年均可选择。
- 播放按钮会逐年自动推进，到 2026 后回到 1797。
- “跳转年份”允许输入年份，并限制在 1797—2026 范围内。
- 关键时间点：1797、1830、1914、1948、1979、2011、2026。
- 关键年份标签按真实年份比例定位，不是平均分布：`(年份 - 1797) / 229 × 100%`。
- 年份主显示仅显示数字，不显示“公元”。
- 点击国家或地区后，地图缩放并显示对应政权、首府、性质和历史说明。
- 区域总览显示“历史概述”；进入国家后标题切换为“政权概述 / REGIME”。
- 1797—1885 年使用概略 SVG 示意边界，并按政权归属着色。
- 1886—1999 年按 CShapes 2.0 有效期区间加载该年 1 月 1 日边界快照；每年每国先选唯一有效时间 cohort，再合并同 cohort 的多块多边形，避免边界重叠与过渡年份闪烁；1886—1923 与 1924—1999 分文件维护。
- 2000—2026 年切换到 Natural Earth 5.1.1 的 1:50m 现代边界参考数据。
- 西撒哈拉与巴勒斯坦使用争议地区纹理和虚线标示。
- 地中海与红海标注置于 SVG 国家图层上方、无边框；已完成手动校准并将最终位置固化在 `styles.css`，进入国家详情时随选中状态隐藏，不再提供临时平移控件。
- 年份切换时，地图大标题始终使用该历史时期的标题（`eras[].map`），不显示“YYYY 年中东与北非国家边界”这类技术性标题。
- 国家悬浮／聚焦时，国家名称同步上浮；同时以按年份切换的本地 Wikimedia Commons 高清历史 SVG/PNG 国旗完整覆盖国家区块，使用 `xMidYMid slice` 居中裁切填充（不拉伸），不叠加褶皱、光泽或颜色滤镜。旗帜索引与许可提示见 `assets/flags/SOURCES.md`。
- 点击国家后，侧边栏标题使用 `formal-names.js` 中按年份匹配的正式国名；南也门独立前显示“亚丁保护国”，独立后地图标签显示“南也门”，正式国名按时期显示“南也门人民共和国”或“也门民主人民共和国”。
- 右侧故事面板随工作区自然延伸到页面底部，取消内部 `overflow-y` 滚动条，由页面统一滚动。
- 重要历史事件可跳转到对应中文维基百科页面。
- 支持键盘操作：国家路径可聚焦，并可用 Enter 或空格选择。
- 1886—1931 年不显示“沙特阿拉伯”现代轮廓，改用汉志、舍迈尔、内志历史上下文图层；西属摩洛哥在 1886—1911 由明确标注的上下文 cohort 补齐，西撒哈拉在缺口年份并入完整历史主张轮廓。
- 西属摩洛哥多块几何会按 SVG 子路径分别生成国旗；国家详情右上角的国旗调试面板只针对当前国家，支持缩放、平移、地图背景色和国旗透明区域填充色调整，浏览器保存并可导出 JSON；红海／地中海位置已永久写入样式表。
- 移动端会改为单栏布局，并隐藏部分时间轴标签以避免拥挤。

## 5. JavaScript 数据结构

### `meta`

国家／地区元数据。键为内部 ID，值包含中文名称与地区名称列表。

### `eras`

区域时代分段。每项包含：

- `from`、`to`：起止年份
- `title`：时代名称
- `native`：本地语言标题
- `badge`：时代标签
- `map`：地图标题
- `summary`：历史概述
- `events`：重要事件数组

### `overrides`

重点国家在不同时段的政权资料，数组项格式：

`[起始年, 结束年, 政权名称, 说明, 首府／政治中心, 政权性质]`

### 地图逻辑

- `setMapMode(year)`：1886 年起启用按年份选择的真实边界图层。
- `historicalStyles(year)`：按年份与政权归属设置历史地图颜色和附属状态；颜色通过稳定的政权键映射，跨年度不因路径顺序变化而重排，奥斯曼帝国与土耳其共和国明确使用不同键。
- `buildBoundaryMap(year)`：从 1886—1923、1924—1999 两组 CShapes 数据、亚丁／北也门补充层或 `window.MENA_2026` 创建 SVG 路径。
- `choose(id)`：选择国家、缩放地图并刷新右侧面板。
- `overview()`：返回区域总览。
- `render()`：统一刷新年份、标题、摘要、事实、事件和地图状态。

## 6. 历史分段

当前区域历史概述分为：

1. 1797—1829：帝国、行省与地方王朝
2. 1830—1913：殖民扩张与改革时代
3. 1914—1947：大战、委任统治与民族国家形成
4. 1948—1978：共和国、革命与冷战
5. 1979—2010：伊斯兰革命、战争与新秩序
6. 2011—2026：起义、内战与多极竞争

## 7. 内容与制图口径

- 1886—1999 年边界来源：CShapes 2.0；按每年 1 月 1 日快照。1886—1937 亚丁保护地、1886—1911 西属摩洛哥与 1886—1917 北也门为明确标注的 contextual 补充。汉志轮廓采自 ArcGIS Kingdom of Hejaz FeatureServer 22，并以 OpenGulf Harita 93677、Arabia 1914 和美国国会图书馆《Arabia》（1920）交叉校核；舍迈尔与内志使用 OpenGulf 的 QGIS Najd 图层、Haydari 地名层及上述历史地图，不再以 CShapes 作为其唯一参考。
- 2000—2026 年边界来源：Natural Earth 5.1.1，1:50m Cultural Vectors。
- 1797—1885 年边界仍是概略示意，不应当作学术地图或领土主张引用。
- 新增或修改历史内容时，应明确年份范围；同一国家同一年不得保留相互冲突的时间 cohort，除非它们是同一 cohort 的合法多块多边形。
- 国旗覆盖层是历史语境标记，不替代边界数据的法理／实际控制口径；保护国、委任统治和奥斯曼地方行政时期如无统一陆上旗帜，使用已注明的上下文回退。巴勒斯坦托管地使用海事旗（ensign）。
- 新增地区时，需要同时检查 `meta`、历史 SVG、2026 数据映射、颜色逻辑和右侧面板资料。

## 8. 修改规则

- 延续现有视觉风格，不进行无关重设计。
- 网站成品直接修改根目录文件，并同步到 `dist/` 副本；本项目没有额外构建目录。
- 时间轴标签必须使用真实年份比例，不能使用 `space-between` 平均排列。
- 保持 1886—1999 历史真实地图、2000—2026 现代参考地图与早期示意地图之间的状态切换。
- 保持争议地区的独立样式与资料说明。
- 不删除已有无障碍标签、键盘事件或移动端适配。
- 修改后至少检查：HTML 引用文件存在、JavaScript 语法、桌面时间轴、移动端布局、1797 与 2026 两个端点。

## 9. 当前完成记录

- 建立 1797—2026 互动历史地图原型。
- 历史示意地图按政权归属着色。
- 接入 2026 年 Natural Earth 精确边界。
- 接入 1886—1999 年 CShapes 2.0 历史边界有效期数据，并按年度切换。
- 以 OpenGulf Harita 93677 QGIS、Arabia 1914 和美国国会图书馆《Arabia》（1920）交叉校核汉志、舍迈尔与内志上下文几何，并在界面资料说明中列出链接。
- 固化红海／地中海标注位置，移除临时海域调试控件；桌面端地图工作区改用固定网格行，确保 1979—1999 与其他年份窗口高度一致。
- 将简笔画旗帜替换为按年份映射的本地 Wikimedia Commons 历史 SVG/PNG 资源，并同步复制到 `dist/assets/flags/`；具体出处见 `assets/flags/SOURCES.md`。
- 修正底部时间轴关键年份与实际节点的对应位置。
- 删除时间轴年份旁的“公元”。
- 区域总览使用“历史概述”，国家详情使用“政权概述”。

## 10. 后续对话的最短启动方式

1. 阅读本文件。
2. 仅按任务需要查看 `dist/index.html`、`dist/styles.css`、`dist/app.js` 或 `dist/mena-2026.js`。
3. 默认直接在 `mena-historical-altas` 修改，并同步根目录与 `dist/`。
4. 用户说“同步到 GitHub”时，不要上传或推送，由用户手动操作。
